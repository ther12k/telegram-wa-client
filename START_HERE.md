# Start Here

1. Read `PROJECT_STATE.md`.
2. Run the integrated design with `bun install && bun run dev`.
3. Review `UI_INTEGRATION.md` before changing UI components.
4. Read `AGENT.md` before assigning development to an agent.
5. Use `PRD.md` as the product authority.
6. Run `bun run check` before committing.
